node_modules/
dist/
coverage/

# The staged publishable package. `pnpm package` rebuilds it from dist/ and
# apps/web/dist, so a checked-in copy could only ever be a stale one.
apps/agentplexd/release/
*.tsbuildinfo
.env
.env.local
.DS_Store

# Agent tooling's local configuration and the worktrees it checks this
# repository out into, inside itself.
.claude/
