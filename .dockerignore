**/node_modules
**/dist
**/coverage
# The staged package. The `package` stage assembles its own from the build it
# has just made; one carried in from a laptop could only be older than that.
apps/agentplexd/release
.git
.env
.env.local

# docs/ is deliberately not excluded. The check container runs prettier over
# the same tree a laptop does, and a file kept out of the context is a file
# prettier never sees.
