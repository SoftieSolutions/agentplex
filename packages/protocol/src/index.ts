/**
 * The wire contract shared by `agentplexd` and the web client.
 *
 * The scaffold establishes the package and the boundary; the frame types and
 * the parsers arrive with AGX-10.
 */
export const PACKAGE_NAME = '@agentplex/protocol';
